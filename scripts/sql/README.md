Collection of common useful SQL scripts

Example:
```bash
docker compose exec -T -u postgres postgres psql -U postgres -d mapswipe < scripts/sql/db-size.sql
```
> [!IMPORTANT]
> Make sure to change Postgres user and Postgres database accordingly
